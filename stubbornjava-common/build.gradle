dependencies {
    compile project(':stubbornjava-undertow')

    // The production code uses the SLF4J logging API at compile time
    compile loggingLib

    // Jackson libs
    compile jacksonLib

    // Metrics 
    compile metricsLib
    
    // Guava
    compile guavaLib

    // Configuration
    compile typesafeConfigLib

    compile handlebarsLib

    compile hikariCpLib
    
    compile joolLib
    
    testCompile junitLib
    testCompile hsqldbLib
}
